<?xml version="1.0" encoding="utf-8"?>
<manifest package="kr.webgirls.sunrin.touchstudy"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LockScreen"></activity>
        <activity android:name=".MistakeQuestionsList"></activity>
        <activity android:name=".AllQuestionsList"></activity>
        <service android:name=".LockService"></service>
    </application>

</manifest>